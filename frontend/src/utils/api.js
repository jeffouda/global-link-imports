const BASE_URL = "http://localhost:5555";

// Export it as a named export (for import { BASE_URL } ...)
export { BASE_URL };

// Export it as a default export (for import BASE_URL ...)
export default BASE_URL;
